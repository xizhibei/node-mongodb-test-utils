version: '2'
services:
  primary:
    image: mongo:${MONGO_VERSION}
    command: mongod  --noprealloc --smallfiles --replSet ${REPLSET_NAME}
  secondary:
    image: mongo:${MONGO_VERSION}
    command: mongod  --noprealloc --smallfiles --replSet ${REPLSET_NAME}

networks:
  default:
    driver: bridge
